services:
  - type: web
    name: classical-backend
    env: python
    repo: "<GIT_REPO_URL>"
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
